# Generated by simit. Manual edits will be reported as ci=drift.
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: atlas
    container: rust:1.88-bookworm
    steps:
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4

      - name: Install Rust components
        run: rustup component add clippy rustfmt

      - name: Cache cargo bin (tools)
        uses: https://code.forgejo.org/actions/cache@v4
        with:
          path: ~/.cargo/bin
          key: cargo-bin-${{ runner.os }}-${{ hashFiles('.forgejo/workflows/ci.yaml', '.github/workflows/ci.yaml') }}

      - name: Test all features
        run: cargo test -p detritus-client --all-features

      - name: Test no default features
        run: cargo test -p detritus-client --no-default-features

      - name: Clippy all features
        run: cargo clippy -p detritus-client --all-targets --all-features -- --deny warnings

      - name: Clippy no default features
        run: cargo clippy -p detritus-client --all-targets --no-default-features -- --deny warnings

      - name: Package crate
        run: cargo package -p detritus-client --allow-dirty
